import { BannerDetail } from '@/components/admin/banner/detail';

const BannerDetailPage = ({ params }: { params: { id: string } }) => {
  return <BannerDetail />;
};

export default BannerDetailPage;
