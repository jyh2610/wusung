import { MemberShip } from '@/entities/UserManage';
import { pageContainer } from './index.css';

export default function JoinMemberShip() {
  return (
    <section className={pageContainer}>
      <MemberShip />
    </section>
  );
}
